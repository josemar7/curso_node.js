<%- include('includes/head.ejs') %>
<main>
    <% if(users.length > 0) { %>
        <% for (let user of users) { %>
            <ul>
                <li><%= user.name %></li>
            </ul>
        <% } %>
    <% } else { %>
        <h1>Not users found</h1>
    <% } %>
</main>
<%- include('includes/end.ejs') %>
